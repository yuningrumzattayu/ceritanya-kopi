<%- include("partials/navbarGuest") %>

<style>
  html, body {
    margin: 0;
    padding: 0;
    height: 100%;
    overflow-x: hidden;
  }

  /* Navbar jadi transparan & absolute supaya tidak makan ruang layout */
  body > nav {
    position: absolute !important;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: transparent !important;
    box-shadow: none !important;
  }

  /* Hero betul-betul full viewport — tidak ada sisa */
  .hero-section {
    position: relative;
    width: 100%;
    height: 100vh;          /* full tinggi layar */
    background-image: url("/images/header.png");
    background-size: cover;
    background-position: center top;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin: 0;
    padding: 0;
  }

  /* Overlay gelap */
  .hero-section::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
  }

  /* Logo card */
  .hero-logo-wrap {
    position: relative;
    z-index: 2;
    background: #fdf6ee;
    border-radius: 12px;
    padding: 12px 18px;
    margin-bottom: 24px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.25);
  }

  .hero-logo-wrap img {
    width: 110px;
    height: auto;
    display: block;
  }

  /* Teks */
  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: #fff;
    padding: 0 1rem;
  }

  .hero-content h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    font-style: italic;
    text-shadow: 0 2px 8px rgba(0,0,0,0.6);
    margin-bottom: 6px;
  }

  .hero-content .tagline {
    font-size: 1.15rem;
    font-weight: 600;
    font-style: italic;
    color: #f0e6d3;
    text-shadow: 0 1px 4px rgba(0,0,0,0.5);
    margin-bottom: 14px;
  }

  .hero-content .description {
    font-size: 1rem;
    color: #e8ddd0;
    text-shadow: 0 1px 4px rgba(0,0,0,0.5);
    margin-bottom: 32px;
  }

  /* Tombol */
  .hero-btn-login,
  .hero-btn-register {
    display: inline-block;
    padding: 10px 36px;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: transform 0.15s, box-shadow 0.15s;
    background-color: #6b8f5e;
    color: #fff;
    border: 2px solid #6b8f5e;
  }

  .hero-btn-login  { margin-right: 12px; }

  .hero-btn-login:hover,
  .hero-btn-register:hover {
    background-color: #577549;
    border-color: #577549;
    color: #fff;
    transform: translateY(-2px);
    box-shadow: 0 6px 18px rgba(0,0,0,0.3);
  }

  /* Footer langsung nempel di bawah hero, hapus margin */
  footer {
    margin-top: 0 !important;
  }
</style>

<section class="hero-section">
  <div class="hero-logo-wrap">
    <img src="/images/logo.png" alt="Ceritanya Kopi Logo" />
  </div>

  <div class="hero-content">
    <h1>Welcome to Ceritanya Kopi</h1>
    <p class="tagline">Fresh Coffee, Fresh Mood</p>
    <p class="description">Enjoy your favorite coffee anytime and anywhere.</p>
    <a href="/login"    class="hero-btn-login">Login</a>
    <a href="/register" class="hero-btn-register">Register</a>
  </div>
</section>

<%- include("partials/footer") %>
